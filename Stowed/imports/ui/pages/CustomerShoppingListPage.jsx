import "../Global.css";

/**
 * SHOPPING LIST
 * A customer's own shopping list, held for the browser session rather than an account.
 *
 * TODO for team:
 *  - Let a customer collect items into a list without an account
 *  - Persist the list for the session alongside the organisation code
 *  - Show where each item sits in the store
 */

export function CustomerShoppingListPage() {
  return (
    <div className="customer-page">
      <h1 className="customer-page-title">Shopping List</h1>
    </div>
  );
}
