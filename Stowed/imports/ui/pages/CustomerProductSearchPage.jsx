import "../Global.css";

/**
 * PRODUCT SEARCH
 * Search the organisation's catalogue as a customer - read-only, no create or edit affordances.
 *
 * TODO for team:
 *  - Publish org-scoped products for anonymous clients
 *  - Search and filter the catalogue (fuse.js is already a dependency)
 *  - Link each result through to a read-only product detail view
 */

export function CustomerProductSearchPage() {
  return (
    <div className="customer-page">
      <h1 className="customer-page-title">Product Search</h1>
    </div>
  );
}
