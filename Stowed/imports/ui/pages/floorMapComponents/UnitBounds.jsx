/**
 * rectBounds.js
 * 
 * Defines a type that will hold domain and range boundaries to enable collision detection.
 * May be extended in the future to hold other shape structures (e.g. circles)
 */
export const rectBounds = {
    /** @type { dom: { lower: x, upper: x + width }, ran: { lower: y, upper: y + height } } */
};